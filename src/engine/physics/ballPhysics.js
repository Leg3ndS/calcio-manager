/** Ball Physics V2 - deterministic ground/air motion. */
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
const F=.01, MAX=.99, G=9.81;
function ensure(b){b.position??={x:b.x??.5,y:b.y??.5};b.velocity??={x:b.velocityX??0,y:b.velocityY??0};b.velocityX??=b.velocity.x;b.velocityY??=b.velocity.y;b.height=Number.isFinite(b.height)?b.height:0;b.verticalVelocity=Number.isFinite(b.verticalVelocity)?b.verticalVelocity:0;b.spin=Number.isFinite(b.spin)?b.spin:0;return b;}
export function launchBall({ball,from,to,speed=.25,height=0,spin=0,state='in_flight'}){ensure(ball);const s=from??ball.position,e=to??s,dx=e.x-s.x,dy=e.y-s.y,d=Math.hypot(dx,dy)||1;ball.x=s.x;ball.y=s.y;ball.position={...s};ball.velocityX=dx/d*speed;ball.velocityY=dy/d*speed;ball.velocity={x:ball.velocityX,y:ball.velocityY};ball.height=Math.max(0,height);ball.verticalVelocity=ball.height>0?Math.sqrt(2*G*ball.height):0;ball.spin=spin;ball.state=state;ball.ownerId=null;ball.targetX=e.x;ball.targetY=e.y;return ball;}
export function updateBallPhysics(ball,dt=.1){if(!ball)return null;const b=ensure(ball),t=clamp(Number(dt)||0,0,.1);if(b.ownerId){b.velocityX=b.velocityY=0;b.velocity={x:0,y:0};b.state='controlled';return b;} if(!['in_flight','rolling','free','loose','contested'].includes(b.state))return b;const drag=b.height>0?.985:.92;b.velocityX*=Math.pow(drag,t*10);b.velocityY*=Math.pow(drag,t*10);if(b.height>0||b.verticalVelocity!==0){b.height+=b.verticalVelocity*t;b.verticalVelocity-=G*t*.02;if(b.height<=0){b.height=0;b.verticalVelocity=0;b.state='rolling';}}b.x=clamp((b.x??b.position.x)+b.velocityX*t,F,MAX);b.y=clamp((b.y??b.position.y)+b.velocityY*t,F,.99);b.position={x:b.x,y:b.y};b.velocity={x:b.velocityX,y:b.velocityY};if(Math.hypot(b.velocityX,b.velocityY)<.001&&b.height<=0){b.velocityX=b.velocityY=0;b.velocity={x:0,y:0};b.state='free';}return b;}
